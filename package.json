{
  "name": "oasis-markets-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @oasis/api dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
